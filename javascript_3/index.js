// All about maths

let x = -23.2;

let y = 2;
let z = 34345;

// z = Math.floor(x);// round down 
// z = Math.ceil(x);
// z=Math.trunc(x); // removes the decimal
// z= Math.pow(x , y);
// z=Math.cos(x);
// z=Math.tan(x);
// z=Math.abs(x); // gives positive value
// z=Math.sign(x); // tells the sign of number it's like positive or negative or 0;

// z=Math.log(x)

let max = Math.max(x ,y , z )
let min = Math.min(x , y, z);
console.log(max);
console.log(min);